# Se utilizar a biblioteca de maneira local (home), deve-se descomentar a seguinte linha:
# module: /var/home/pedro/.local/lib/libaetpkss.so

# Se instalar o rpm, deve-se descomentar a linha seguinte:
# module: /usr/lib64/libaetpkss.so
